# ASSETS 

- confindr containers (conda build inside docker container). confindr.yml gives the option for building. It can be used for a normal conda build, the path of build can be provided with "prefix:"
